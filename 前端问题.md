# 前端问题.md

> **版本：v3.0.0** | 更新日期：2026-06-20

> 本文档记录前端开发的需求、问题和技术对接说明。

---

## 一、基础配置

### 1.1 .env 配置

```env
VITE_DEV_PORT=5173
VITE_API_HOST=localhost
VITE_API_PORT=8080
VITE_API_PREFIX=/v1/api
VITE_API_BASE_URL=http://localhost:8080
```

### 1.2 API URL 拼接规则

```javascript
// 健康检查（不走 prefix）
const healthUrl = `${VITE_API_BASE_URL}/health`;
// → http://localhost:8080/health

// 其他 API（走 prefix）
const loginUrl = `${VITE_API_BASE_URL}${VITE_API_PREFIX}/auth/login`;
// → http://localhost:8080/v1/api/auth/login
```

---

## 二、登录与认证

### 2.1 登录接口

```
POST /v1/api/auth/login
Content-Type: application/json

{
  "email": "user@example.com",
  "password": "123456"
}
```

**成功响应：**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "mqc8d5b497u5",
    "nickname": "小明",
    "email": "user@example.com",
    "avatar": "/static/icons/avatar.png",
    "role": "user"
  }
}
```

### 2.2 管理员登录

管理员使用相同的登录接口，登录成功后检查 `user.role === "admin"`。

```
POST /v1/api/auth/login
{
  "email": "admin@s-lan.com",
  "password": "Admin@2026!"
}
```

**成功响应：**
```json
{
  "token": "eyJhbGciOiJIUzI1NiIs...",
  "user": {
    "id": "mqkzxpkzv1wh",
    "nickname": "管理员",
    "email": "admin@s-lan.com",
    "role": "admin"
  }
}
```

### 2.3 登录后跳转逻辑

```javascript
const { token, user } = await login(email, password);

// 保存 Token
localStorage.setItem('token', token);
localStorage.setItem('user', JSON.stringify(user));

// 根据角色跳转
if (user.role === 'admin') {
  window.location.href = '/pages/admin/dashboard';
} else {
  window.location.href = '/home';
}
```

---

## 三、用户信息

### 3.1 获取用户信息

```
GET /v1/api/user/profile
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "id": "mqc8d5b497u5",
  "nickname": "小明",
  "email": "user@example.com",
  "avatar": "/static/icons/avatar.png"
}
```

**注意**：`id` 是字符串类型，格式为 `timestamp + random`。

---

## 四、游戏版本

### 4.1 获取版本列表

```
GET /v1/api/versions
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "list": [
    {
      "_id": "6a355c0623b1d8bdc31493a5",
      "versionId": 1,
      "name": "游戏王日文",
      "lang": "日文",
      "logo": "/uploads/versions/logo.png"
    }
  ]
}
```

**注意**：
- `logo` 为空时使用默认图标：`/static/icons/version_logos/logo_yugioh_jp.png`
- 版本数据现在从数据库读取

---

## 五、罕贵度与品相

### 5.1 获取罕贵度列表（带默认值）

```
GET /v1/api/rarities?versionId=1
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "list": ["QCSER", "PSER", "SER", "UR", "SR", "R", "N"],
  "default": "QCSER"
}
```

### 5.2 获取品相列表（带默认值）

```
GET /v1/api/grades?versionId=1
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "list": [
    { "name": "99品", "desc": "完美全新", "hasSub": false },
    { "name": "9品", "desc": "近新优品", "hasSub": false },
    { "name": "评级卡", "desc": "", "hasSub": true }
  ],
  "default": "9品"
}
```

### 5.3 获取评级卡子选项

```
GET /v1/api/grades/psa
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "list": ["PSA 10", "PSA 9", "PSA 8", "PSA 7", "PSA 6", "PSA 5", "PSA 4", "PSA 3", "PSA 2", "PSA 1"]
}
```

---

## 六、库管理

### 6.1 获取本地库列表

```
GET /v1/api/libraries/local
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "list": [
    { "code": "A001", "cardCount": 10 },
    { "code": "A002", "cardCount": 60 }
  ]
}
```

### 6.2 创建新库

```
POST /v1/api/libraries
Authorization: Bearer <token>
Content-Type: application/json

{
  "type": "local",
  "name": "A021"
}
```

**成功响应（201）：**
```json
{
  "code": "A021",
  "id": "MX00000021",
  "cardCount": 0
}
```

---

## 七、卡牌管理

### 7.1 获取库中卡牌列表

```
GET /v1/api/libraries/:code/cards?type=local&page=1&pageSize=20
Authorization: Bearer <token>
```

**成功响应：**
```json
{
  "list": [
    {
      "id": "...",
      "jpName": "閃刀姫－レイ",
      "cnName": "闪刀姬-零",
      "code": "QCAC-JP059",
      "rarity": "QCSER",
      "condition": "9品",
      "serial": "A001-0001",
      "img": "",
      "libraryCode": "A001",
      "versionId": 1
    }
  ],
  "total": 8,
  "page": 1,
  "pageSize": 20
}
```

### 7.2 创建卡牌

```
POST /v1/api/cards
Authorization: Bearer <token>
Content-Type: application/json

{
  "libraryCode": "A001",
  "jpName": "閃刀姫－レイ",
  "cnName": "闪刀姬-零",
  "code": "QCAC-JP059",
  "rarity": "QCSER",
  "condition": "9品",
  "versionId": 1
}
```

**成功响应：**
```json
{
  "id": "...",
  "serial": "A001-0001",
  "img": ""
}
```

### 7.3 删除卡牌

```
DELETE /v1/api/cards/:id
Authorization: Bearer <token>
```

**成功响应：** `{ "success": true }`

---

## 八、卡牌信息查询（card-info）

> 拍照识别后，通过 cardCode 查询卡牌主数据获取真实名称和卡图。

### 8.1 查询单张卡牌信息

```
GET /v1/api/card-info/:cardCode
Authorization: Bearer <token>
```

**成功响应（有数据）：**
```json
{
  "cardCode": "QCAC-JP001",
  "versionId": 1,
  "jpName": "青眼の白龍",
  "cnName": "青眼白龙",
  "imageUrl": "/uploads/cards/QCAC-JP001.png",
  "rarity": "SER"
}
```

**成功响应（未收录）：**
```json
{
  "cardCode": "UNKNOWN-001",
  "versionId": null,
  "jpName": null,
  "cnName": null,
  "imageUrl": null,
  "rarity": null
}
```

### 8.2 批量查询卡牌信息

```
POST /v1/api/card-info/batch
Authorization: Bearer <token>
Content-Type: application/json

{
  "cardCodes": ["QCAC-JP001", "QCAC-JP002", "QCAC-JP003"]
}
```

**成功响应：**
```json
{
  "data": [
    { "cardCode": "QCAC-JP001", "jpName": "青眼の白龍", "cnName": "青眼白龙", "imageUrl": "...", "rarity": "SER" },
    { "cardCode": "QCAC-JP002", "jpName": "真紅眼の黒竜", "cnName": "真红眼黑龙", "imageUrl": "...", "rarity": "UR" },
    { "cardCode": "QCAC-JP003", "jpName": null, "cnName": null, "imageUrl": null, "rarity": null }
  ]
}
```

> 单次最多 100 条。未收录的卡牌返回 `null` 字段，前端继续显示占位数据。

### 8.3 拍照识别后自动查询

```javascript
async function doRecognizeAndEnrich(imageFile, versionId) {
  // 1. 拍照识别
  const recognizeResult = await recognizeCard(imageFile, versionId);
  const cardCode = recognizeResult.code;

  // 2. 查询卡牌信息
  const cardInfo = await getCardInfo(cardCode);

  // 3. 合并数据
  if (cardInfo.jpName) {
    // 有数据，使用真实名称和卡图
    return {
      ...recognizeResult,
      jpName: cardInfo.jpName,
      cnName: cardInfo.cnName,
      img: cardInfo.imageUrl,
      rarity: cardInfo.rarity
    };
  } else {
    // 无数据，保持识别结果，显示占位图
    return {
      ...recognizeResult,
      img: '/static/icons/card_placeholder.png'
    };
  }
}
```

---

## 九、拍照识别

### 9.1 识别卡牌

```
POST /v1/api/vision/recognize
Authorization: Bearer <token>
Content-Type: multipart/form-data

image: <file>
versionId: 1
```

**成功响应：**
```json
{
  "jpName": "閃刀姫－レイ",
  "cnName": "闪刀姬-零",
  "code": "QCAC-JP059",
  "rarity": "QCSER",
  "confidence": 0.95
}
```

---

## 十、错误处理

### 9.1 HTTP 状态码

| 状态码 | 含义 | 处理建议 |
|--------|------|----------|
| 200 | 成功 | 正常处理响应 |
| 201 | 创建成功 | 正常处理响应 |
| 400 | 参数错误 | 提示用户检查输入 |
| 401 | 未授权 | 清除 Token，跳转登录页 |
| 403 | 禁止访问 | 非管理员用户，禁止访问管理员接口 |
| 404 | 资源不存在 | 提示用户资源不存在 |
| 409 | 冲突 | 根据错误信息提示（如版本有关联卡牌无法删除） |
| 500 | 服务器错误 | 提示用户稍后重试 |

### 9.2 统一错误处理

```javascript
async function apiRequest(method, path, data) {
  try {
    const result = await api.request(method, path, data);
    return { success: true, data: result };
  } catch (error) {
    if (error.message.includes('Unauthorized')) {
      // Token 过期，跳转登录
      localStorage.removeItem('token');
      localStorage.removeItem('user');
      window.location.href = '/login';
      return { success: false, redirect: '/login' };
    }

    if (error.message.includes('Forbidden')) {
      // 非管理员，禁止访问
      window.location.href = '/home';
      return { success: false, error: '无权限访问' };
    }

    return { success: false, error: error.message };
  }
}
```

---

## 十一、管理员后台需求

### 待对接的后端功能（P0-P2）

| 优先级 | 功能 | 接口 | 状态 |
|:---:|------|------|:---:|
| P0 | ~~视觉识别~~ | `POST /vision/recognize` | ✅ 已实现 |
| P2 | ~~版本 Logo 上传~~ | `POST /admin/versions/:id/logo` | ✅ 已实现 |
| P2 | ~~删除版本~~ | `DELETE /admin/versions/:id` | ✅ 已实现 |
| P1 | 第三方云端 | 标准接口规范 | 🆕 待实现 |
| P1 | 视觉识别 OCR | 替换模拟数据 | 🆕 待实现 |

> ✅ = 后端已实现，接口文档见下方 | 🆕 = 待实现

---

## 十二、后端已实现接口文档（前端对接指南）

### 12.1 认证接口

#### 12.1.1 用户注册
```
POST /v1/api/auth/register
Content-Type: application/json

Request:
{
  "email": "user@example.com",
  "password": "password123",
  "inviteCode": "INVITE123"
}

Response (201):
{
  "token": "eyJhbGciOiJIUzI1...",
  "user": {
    "id": "user_id",
    "email": "user@example.com",
    "role": "user"
  }
}

Errors:
- 400: 缺少必填字段
- 409: 邮箱已注册
- 400: 无效邀请码
```

#### 12.1.2 用户登录
```
POST /v1/api/auth/login
Content-Type: application/json

Request:
{
  "email": "user@example.com",
  "password": "password123"
}

Response (200):
{
  "token": "eyJhbGciOiJIUzI1...",
  "user": {
    "id": "user_id",
    "email": "user@example.com",
    "role": "admin"    // 管理员为 "admin"，普通用户为 "user"
  }
}

Errors:
- 400: 缺少必填字段
- 401: 邮箱或密码错误
```

#### 12.1.3 获取当前用户信息
```
GET /v1/api/auth/me
Authorization: Bearer <token>

Response (200):
{
  "id": "user_id",
  "email": "user@example.com",
  "role": "admin"
}
```

---

### 12.2 库管理接口

#### 12.2.1 获取本地库列表
```
GET /v1/api/libraries/local
Authorization: Bearer <token>

Response (200):
{
  "list": [
    { "code": "我的卡组", "cardCount": 25 }
  ]
}
```

#### 12.2.2 获取云端库列表
```
GET /v1/api/libraries/cloud
Authorization: Bearer <token>

Response (200):
{
  "list": [
    { "code": "云端卡组", "cardCount": 10 }
  ]
}
```

#### 12.2.3 获取库详情
```
GET /v1/api/libraries/:code?type=local
Authorization: Bearer <token>

Response (200):
{
  "code": "我的卡组",
  "id": "library_id",
  "name": "我的卡组",
  "type": "local",
  "cardCount": 25
}
```

#### 12.2.4 创建新库
```
POST /v1/api/libraries
Authorization: Bearer <token>
Content-Type: application/json

Request:
{
  "type": "local",
  "name": "新卡组名称"
}

Response (201):
{
  "code": "新卡组名称",
  "id": "library_id",
  "cardCount": 0
}

Errors:
- 400: name 为必填
- 409: 库名已存在
```

#### 12.2.5 更新库
```
PUT /v1/api/libraries/:code
Authorization: Bearer <token>
Content-Type: application/json

Request:
{
  "name": "新名称",
  "type": "cloud"    // 可选：local/cloud
}

Response (200):
{
  "code": "新卡组名称",
  "id": "library_id",
  "name": "新名称",
  "type": "cloud",
  "cardCount": 25
}
```

#### 12.2.6 获取库中卡牌列表
```
GET /v1/api/libraries/:code/cards?type=local&page=1&pageSize=20&rarity=UR&versionId=1
Authorization: Bearer <token>

Query 参数：
- type: local/cloud (必填)
- page: 页码，默认 1
- pageSize: 每页数量，默认 20
- rarity: 筛选罕贵度（可选）
- condition: 筛选品相（可选）
- versionId: 筛选版本（可选）

Response (200):
{
  "list": [
    {
      "id": "card_id",
      "jpName": "闪刀姬",
      "cnName": "闪刀姬-零",
      "code": "QCAC-JP059",
      "rarity": "QCSER",
      "condition": "NM",
      "serial": "SER001",
      "img": "/uploads/cards/xxx.jpg",
      "libraryCode": "我的卡组",
      "versionId": 1
    }
  ],
  "total": 100,
  "page": 1,
  "pageSize": 20
}
```

---

### 12.3 游戏版本接口

#### 12.3.1 获取版本列表
```
GET /v1/api/versions
Authorization: Bearer <token>

Response (200):
{
  "list": [
    { "id": "default-1", "name": "游戏王日文", "lang": "日文", "logo": "/static/icons/version_logos/logo_yugioh_jp.png" },
    { "id": "default-2", "name": "游戏王简中", "lang": "简中", "logo": "/static/icons/version_logos/logo_yugioh_cn.png" }
  ]
}
```

---

### 12.4 视觉识别接口

#### 12.4.1 拍照识别卡牌
```
POST /v1/api/vision/recognize
Authorization: Bearer <token>
Content-Type: multipart/form-data

Form Data:
  image: File       # 卡牌图片文件
  versionId: string # 游戏版本 ID（可选）

Response (200):
{
  "jpName": "閃刀姫－レイ",
  "cnName": "闪刀姬-零",
  "code": "QCAC-JP059",
  "rarity": "QCSER",
  "confidence": 0.95
}

Errors:
- 400: 缺少 image 文件

> 注意：当前返回模拟数据，后续需接入真实 OCR
```

---

### 12.5 管理员接口（需 admin role）

#### 12.5.1 获取版本列表（管理）
```
GET /v1/api/admin/versions
Authorization: Bearer <token>

Response (200):
{
  "list": [
    { "versionId": 1, "name": "游戏王日文", "lang": "日文", "logo": "" },
    { "versionId": 2, "name": "游戏王简中", "lang": "简中", "logo": "" }
  ]
}
```

#### 12.5.2 获取单个版本
```
GET /v1/api/admin/versions/:versionId
Authorization: Bearer <token>

Response (200):
{
  "versionId": 1,
  "name": "游戏王日文",
  "lang": "日文",
  "logo": ""
}
```

#### 12.5.3 创建版本
```
POST /v1/api/admin/versions
Authorization: Bearer <token>
Content-Type: application/json

Request:
{
  "name": "新版本名称",
  "lang": "日文"
}

Response (201):
{
  "versionId": 9,
  "name": "新版本名称",
  "lang": "日文",
  "logo": ""
}
```

#### 12.5.4 更新版本
```
PUT /v1/api/admin/versions/:versionId
Authorization: Bearer <token>
Content-Type: application/json

Request:
{
  "name": "更新后的名称",
  "lang": "英文",
  "logo": "/uploads/versions/new-logo.png"
}

Response (200):
{
  "versionId": 1,
  "name": "更新后的名称",
  "lang": "英文",
  "logo": "/uploads/versions/new-logo.png"
}
```

#### 12.5.5 删除版本
```
DELETE /v1/api/admin/versions/:versionId
Authorization: Bearer <token>

Response (200):
{
  "success": true
}

Errors:
- 404: 版本不存在
- 409: 版本下有卡牌，无法删除
```

#### 12.5.6 上传版本 Logo
```
POST /v1/api/admin/versions/:versionId/logo
Authorization: Bearer <token>
Content-Type: multipart/form-data

Form Data:
  image: File  # 图片文件 (png/jpg/webp, 最大 2MB)

Response (200):
{
  "logoUrl": "/uploads/versions/version-1-1234567890.png"
}

Errors:
- 400: 未提供图片
- 404: 版本不存在
- 400: 文件类型错误
```

#### 12.5.7 获取版本配置
```
GET /v1/api/admin/version-config/:versionId
Authorization: Bearer <token>

Response (200):
{
  "versionId": 1,
  "rarities": {
    "default": ["N", "R", "SR", "UR", "QCSER"],
    "N": ["N", "R", "SR", "UR"],
    "QCSER": ["QCSER"]
  },
  "grades": {
    "default": ["NM", "EX-MT", "MT", "SP", "MP", "HP", "DMG"],
    "N": ["NM", "MT", "EX-MT"],
    "QCSER": ["NM", "MT"]
  }
}
```

#### 12.5.8 更新版本配置
```
PUT /v1/api/admin/version-config/:versionId
Authorization: Bearer <token>
Content-Type: application/json

Request:
{
  "rarities": {
    "default": ["N", "R", "SR", "UR"],
    "N": ["N", "R", "SR", "UR"]
  },
  "grades": {
    "default": ["NM", "MT", "EX-MT"],
    "N": ["NM", "MT"]
  }
}

Response (200):
{
  "versionId": 1,
  "rarities": {...},
  "grades": {...}
}
```

---

### 12.6 测试方法

#### 使用 curl 测试

```bash
# 1. 先登录获取 token
curl -X POST http://localhost:8080/v1/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"admin@s-lan.com","password":"Admin@2026!"}'

# 2. 使用 token 获取版本列表
curl http://localhost:8080/v1/api/versions \
  -H "Authorization: Bearer <上面获取的token>"

# 3. 上传图片测试视觉识别
curl -X POST http://localhost:8080/v1/api/vision/recognize \
  -H "Authorization: Bearer <token>" \
  -F "image=@/path/to/card.jpg"

# 4. 管理员接口测试
curl http://localhost:8080/v1/api/admin/versions \
  -H "Authorization: Bearer <token>"
```

#### 使用 Postman/Apifox

1. 新建 Collection
2. 添加 `POST /v1/api/auth/login` 请求，保存 token
3. 在其他请求的 `Authorization` 头中使用 `Bearer {{token}}`
4. 视觉识别接口：Body → form-data，添加 `image` 文件字段

---

### 12.7 通用说明

| 项目 | 说明 |
|-----|------|
| 认证方式 | `Authorization: Bearer <token>` |
| Content-Type | JSON 接口用 `application/json`，文件上传用 `multipart/form-data` |
| 编码 | UTF-8 |
| 错误格式 | `{ "error": "错误信息" }` |
| 成功格式 | `{ "success": true }` 或对应数据对象 |
| 管理员接口 | 需要 `role: "admin"` 的用户 token |

---

### 12.8 待实现功能（前端可先预留接口）

| 功能 | 说明 | 预估工作量 |
|-----|------|:---:|
| 视觉识别 OCR | 替换模拟数据，接入豆包/其他 OCR | 中 |
| 第三方云端 | 标准接口规范，对接第三方服务器 | 大 |
| 删除库 | `DELETE /libraries/:code` | 小 |
| 卡牌导入导出 | 批量导入导出功能 | 中 |

### 10.1 管理员页面路由

```
/pages/admin/
├── dashboard.vue       # 管理员首页/概览
├── versions.vue        # 游戏版本管理（增删改查）
├── version-config.vue  # 版本配置（罕贵度、品相）
└── cards.vue           # 卡牌信息管理（可选）
```

### 10.2 权限检查

```javascript
// 在管理员页面加载时检查权限
function checkAdminAccess() {
  const user = JSON.parse(localStorage.getItem('user') || '{}');

  if (user.role !== 'admin') {
    window.location.href = '/home';
    return false;
  }
  return true;
}

// 页面加载时调用
onMounted(() => {
  checkAdminAccess();
});
```

### 10.3 游戏版本管理页面

#### 功能需求

1. **列表展示**：展示所有游戏版本
   - 显示字段：versionId、name、lang、logo
   - 支持分页（可选）

2. **新增版本**
   - 表单字段：name（必填）、lang（选填）
   - 调用 `POST /v1/api/admin/versions`

3. **编辑版本**
   - 可编辑：name、lang、logo
   - 调用 `PUT /v1/api/admin/versions/:versionId`

4. **删除版本**
   - 调用 `DELETE /v1/api/admin/versions/:versionId`
   - 有关联卡牌时提示：`Cannot delete version with associated cards`

5. **上传 Logo**
   - 支持 png/jpg/webp，建议 ≤ 2MB
   - 调用 `POST /v1/api/admin/versions/:versionId/logo`

#### UI 建议

```vue
<template>
  <div class="admin-versions">
    <!-- 版本列表 -->
    <div class="version-list">
      <div v-for="version in versions" :key="version.versionId" class="version-item">
        <img :src="version.logo || defaultLogo" class="version-logo" />
        <div class="version-info">
          <h3>{{ version.name }}</h3>
          <p>{{ version.lang }}</p>
        </div>
        <div class="version-actions">
          <button @click="editVersion(version)">编辑</button>
          <button @click="deleteVersion(version.versionId)">删除</button>
        </div>
      </div>
    </div>

    <!-- 新增/编辑弹窗 -->
    <el-dialog v-model="showDialog">
      <el-form>
        <el-form-item label="版本名称">
          <el-input v-model="form.name" />
        </el-form-item>
        <el-form-item label="语言">
          <el-input v-model="form.lang" />
        </el-form-item>
        <el-form-item label="Logo">
          <input type="file" @change="onLogoChange" accept="image/*" />
        </el-form-item>
      </el-form>
    </el-dialog>
  </div>
</template>
```

### 10.4 版本配置管理页面

#### 功能需求

1. **选择版本**：下拉选择要配置的版本

2. **配置罕贵度**
   - 列表编辑：增删改排序
   - 设置默认罕贵度

3. **配置品相**
   - 列表编辑：增删改
   - 每个品相可设置：name、desc、hasSub
   - 设置默认品相

4. **保存配置**
   - 调用 `PUT /v1/api/admin/version-config/:versionId`

#### UI 建议

```vue
<template>
  <div class="version-config">
    <!-- 版本选择 -->
    <el-select v-model="selectedVersionId" @change="loadConfig">
      <el-option v-for="v in versions" :key="v.versionId" :label="v.name" :value="v.versionId" />
    </el-select>

    <!-- 罕贵度配置 -->
    <div class="rarities-config">
      <h4>罕贵度</h4>
      <draggable v-model="config.rarities" item-key="index">
        <template #item="{ element }">
          <div class="rarity-item">
            <span>{{ element }}</span>
          </div>
        </template>
      </draggable>
      <el-select v-model="config.defaultRarity">
        <el-option v-for="r in config.rarities" :key="r" :label="r" :value="r" />
      </el-select>
    </div>

    <!-- 品相配置 -->
    <div class="grades-config">
      <h4>品相</h4>
      <div v-for="(grade, index) in config.grades" :key="index" class="grade-item">
        <el-input v-model="grade.name" />
        <el-input v-model="grade.desc" />
        <el-checkbox v-model="grade.hasSub">有子选项</el-checkbox>
      </div>
      <el-select v-model="config.defaultGrade">
        <el-option v-for="g in config.grades" :key="g.name" :label="g.name" :value="g.name" />
      </el-select>
    </div>

    <!-- 保存按钮 -->
    <el-button type="primary" @click="saveConfig">保存配置</el-button>
  </div>
</template>
```

---

## 十二、常见问题

### Q1: Token 过期怎么办？

```javascript
// 统一在请求拦截器中处理
axios.interceptors.response.use(
  response => response,
  error => {
    if (error.response?.status === 401) {
      localStorage.removeItem('token');
      localStorage.removeItem('user');
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);
```

### Q2: 如何处理版本 Logo？

```javascript
const getLogoUrl = (logo) => {
  if (!logo) {
    return '/static/icons/version_logos/default.png';
  }
  if (logo.startsWith('/')) {
    return `http://localhost:8080${logo}`;
  }
  return logo;
};
```

### Q3: 版本 ID 是数字还是字符串？

- `version.versionId` 是 **数字** 类型
- `card.versionId` 也是 **数字** 类型
- `user.id` 是 **字符串** 类型

---

## 十三、接口汇总表

### 普通用户接口

| 序号 | 方法 | 路径 | 说明 |
|------|------|------|------|
| 1 | POST | `/v1/api/auth/login` | 登录 |
| 2 | POST | `/v1/api/auth/register` | 注册 |
| 3 | GET | `/v1/api/user/profile` | 用户信息 |
| 4 | GET | `/v1/api/versions` | 游戏版本列表 |
| 5 | GET | `/v1/api/libraries/local` | 本地库列表 |
| 6 | GET | `/v1/api/libraries/cloud` | 云端库列表 |
| 7 | POST | `/v1/api/libraries` | 创建库 |
| 8 | GET | `/v1/api/libraries/:code/cards` | 卡牌列表 |
| 9 | POST | `/v1/api/cards` | 创建卡牌 |
| 10 | DELETE | `/v1/api/cards/:id` | 删除卡牌 |
| 11 | GET | `/v1/api/rarities` | 罕贵度列表 |
| 12 | GET | `/v1/api/grades` | 品相列表 |
| 13 | POST | `/v1/api/vision/recognize` | 拍照识别 |

### 管理员接口

| 序号 | 方法 | 路径 | 说明 |
|------|------|------|------|
| A1 | GET | `/v1/api/admin/versions` | 获取所有版本 |
| A2 | GET | `/v1/api/admin/versions/:versionId` | 获取单个版本 |
| A3 | POST | `/v1/api/admin/versions` | 新增版本 |
| A4 | PUT | `/v1/api/admin/versions/:versionId` | 更新版本 |
| A5 | DELETE | `/v1/api/admin/versions/:versionId` | 删除版本 |
| A6 | POST | `/v1/api/admin/versions/:versionId/logo` | 上传 Logo |
| B1 | GET | `/v1/api/admin/version-config` | 所有版本配置 |
| B2 | GET | `/v1/api/admin/version-config/:versionId` | 单版本配置 |
| B3 | PUT | `/v1/api/admin/version-config/:versionId` | 创建/更新配置 |

---

## 十四、参考文档

### 相关文档

- 完整接口文档：`API-frontend.md`
- 后端接口规格：`API.backend.md`
- 后端问题记录：`后端问题.md`

### 管理员接口对接要点

| 功能 | 接口 | 说明 |
|-----|------|------|
| 管理员登录 | `POST /v1/api/auth/login` | 返回 token 需检查 `role === 'admin'` |
| 版本管理-列表 | `GET /v1/api/admin/versions` | 获取所有版本 |
| 版本管理-新增 | `POST /v1/api/admin/versions` | name, lang, logo |
| 版本管理-更新 | `PUT /v1/api/admin/versions/:id` | 部分更新 |
| 版本管理-删除 | `DELETE /v1/api/admin/versions/:id` | 删除版本 |
| 版本管理-Logo上传 | `POST /v1/api/admin/versions/:id/logo` | multipart/form-data |
| 版本配置-获取 | `GET /v1/api/admin/version-config/:versionId` | 获取罕贵度+品相配置 |
| 版本配置-更新 | `PUT /v1/api/admin/version-config/:versionId` | 更新配置 |
| 罕贵度列表 | `GET /v1/api/rarities` | 所有罕贵度 |
| 品相列表 | `GET /v1/api/grades` | 所有品相 |
| 品相子选项 | `GET /v1/api/grades/psa` | PSA 评级选项 |
| 视觉识别 | `POST /v1/api/vision/recognize` | multipart/form-data（后端未实现） |
| 云端库列表 | `GET /v1/api/libraries/cloud` | 第三方云端库 |
| 云端库详情 | `GET /v1/api/libraries/:code?type=cloud` | 云端库卡牌列表 |

### 状态码

| 状态码 | 说明 |
|-------|------|
| 200 | 成功 |
| 400 | 请求参数错误 |
| 401 | 未登录或 token 过期 |
| 403 | 无权限（需要管理员） |
| 404 | 资源不存在 |
| 500 | 服务器错误 |
